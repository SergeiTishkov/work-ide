# Work IDE — the minimal set of dependencies. Heavy libraries (feedparser, bs4,
# rapidfuzz) are avoided deliberately: RSS is parsed with the standard library's
# xml.etree.ElementTree, HTML is cleaned by our own regex stripper
# (tools/common.py:strip_html), and fuzzy string comparison turned out not to be
# needed at all. The fewer dependencies, the more reliably `pip install` works
# years from now.

requests>=2.31,<3
PyYAML>=6.0,<7
pytest>=7.4,<9
