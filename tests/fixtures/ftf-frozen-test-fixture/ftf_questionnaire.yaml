# Вопросник фикстуры — заполнен формально.
#
# У тестовой фикстуры нет живого человека, которого можно опросить. Файл
# существует, чтобы структура папки была полной и `identity.py validate`
# проверял фикстуру теми же правилами, что и настоящие идентичности.
#
# Шаблон с полным списком вопросов — identities/_template/tmpl_questionnaire.yaml;
# как проводить опрос — docs/QUESTIONNAIRE.md. Пример заполненного вопросника
# ищите в любой личной идентичности (`python tools/identity.py list`).
#
# Намеренно без ссылок на конкретную живую идентичность: файлы одной
# идентичности не должны упоминать префиксы другой — на это есть проверка в
# identity.validate(), и она поймала именно эту строку, когда здесь стояла
# прямая ссылка.

schema_version: 1
identity_prefix: ftf
filled_with_user_at: null   # никого не опрашивали: это фикстура
is_fixture: true

note: >
  Значения профиля и критериев этой идентичности — побайтовая копия конфигурации
  проекта на 2026-07-31, к которой откалиброваны тесты. Они НЕ выведены из
  ответов на вопросник и не должны регенерироваться через `identity.py build`.

answers: []
